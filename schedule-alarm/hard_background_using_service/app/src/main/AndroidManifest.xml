<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="kr.co.seoft.schedule_alarm"
    >

    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".util.App"
        android:theme="@style/Theme.Schedulealarm"
        >
        <activity android:name=".example.ExampleActivity" />
        <activity android:name=".example2.ExampleActivity2" />
        <activity android:name=".timer.TimerActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".timer.TimerService" />
        <service android:name=".example.ExampleService" />
        <service android:name=".example2.ExampleService2" />
    </application>

</manifest>